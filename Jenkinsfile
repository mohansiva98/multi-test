pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building on dev branch'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying to Dev environment'
            }
        }
    }
}
